openapi: 3.0.0
info:
  title: Dummy API
  version: 2.0.0
paths:
  /hello:
    get:
      summary: Returns a greeting
      responses:
        '200':
          description: A greeting message
          content:
            application/json:
              schema:
                type: object
                properties:
                  minor:
                    type: string
